<div class="row">
	<div class="col-3"></div>

	<div class="col-6">
		<% @pages.roots.each do |page| %>
			<%= show_tree(page) %>
		<% end %>
		</br>
		<%= link_to 'New page', new_page_path, class: 'btn btn-primary' %>
	</div>
	<div class="col-3"></div>
</div>
